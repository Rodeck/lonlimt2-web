# ── Patcher Download ─────────────────────────────────────────
# Full URL to the game client patcher executable
PATCHER_URL=https://fairmt2.pl/FairMT2.Patcher.exe

# ── Database (Registration user — EXECUTE-only on sp_create_player_account) ──
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=web_register
DB_PASSWORD=change_me_strong_password
DB_NAME=account

# ── Server ────────────────────────────────────────────────────
PORT=8080
